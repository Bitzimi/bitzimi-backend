name: Fix transaction hydration
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Informational manual-only workflow
        run: echo 'Automatic transaction source rewriting is disabled; run manually when explicitly audited.'
